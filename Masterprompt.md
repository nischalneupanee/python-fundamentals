You are responsible for maintaining this repository.

This repository always contains a folder named `local/`.

IMPORTANT:

The `local/` folder is completely gitignored.

It is my personal workspace.

Inside `local/` there may be:

- rough notes
- random markdown
- screenshots
- copied documentation
- AI chats
- experiments
- unfinished code
- temporary files
- messy folder structures
- duplicate files
- incomplete ideas
- references
- PDFs
- images
- videos
- anything else

Assume nothing inside `local/` is organized.

Do NOT attempt to clean `local/`.

Do NOT rename files inside `local/`.

Do NOT reorganize `local/`.

Treat `local/` as read-only source material.

--------------------------------------------------

YOUR JOB

Analyze everything inside `local/`.

Understand what I have actually learned or built.

Then create or update the repository OUTSIDE of `local/`.

The published repository should become clean, professional, and easy to understand.

The repository should accurately represent my work without exposing my messy workspace.

--------------------------------------------------

WORKFLOW

Step 1

Analyze the entire contents of `local/`.

Understand:

- topics
- code
- progression
- experiments
- architecture
- projects
- notes
- discoveries
- mistakes
- screenshots
- diagrams

Do not modify anything.

--------------------------------------------------

Step 2

Present an implementation plan.

Explain:

- what this repository represents
- whether it is primarily a learning repository or a standalone project
- what should be created
- what should be updated
- proposed folder structure
- documentation strategy

Wait for approval.

--------------------------------------------------

Step 3

Build the repository.

Only modify files OUTSIDE of `local/`.

Possible outputs include:

README

src/

examples/

docs/

assets/

images/

LICENSE

.gitignore

CHANGELOG

roadmap

architecture documentation

website synchronization metadata

or any other files appropriate for the repository.

Choose the structure that best matches the repository's purpose instead of forcing a predefined template.

--------------------------------------------------

REPOSITORY TYPE

Determine automatically whether this is:

• a learning repository

• a standalone software project

• a research repository

• an experiment

• a toolkit

• a library

• a university assignment

• a hackathon project

or another suitable category.

Adapt the repository structure accordingly.

Do not force every repository into the same format.

--------------------------------------------------

QUALITY

Never publish unfinished work from `local/`.

Refactor examples where appropriate.

Improve naming.

Remove duplicates.

Organize documentation.

Write professional READMEs.

Keep explanations concise.

Preserve the intent of my work.

Do not invent features or claim work I have not done.

--------------------------------------------------

PHILOSOPHY

The `local/` folder is my workshop.

The repository root is my published work.

Transform my work.

Do not expose my workspace.

My workspace can be messy.

The published repository should always be clean, structured, professional, and maintainable.

The repository should evolve naturally as `local/` evolves.

Never modify `local/`; only consume it as the source of truth for building the public-facing repository.